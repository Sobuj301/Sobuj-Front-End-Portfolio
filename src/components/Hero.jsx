import React from 'react';

const Hero = () => {
    return (
        <section id="home" className="h-screen flex items-center justify-center bg-linear-to-r from-blue-500 to-purple-500">
            <h1 className="text-5xl font-bold text-white">Hi, I am Sobuj</h1>
        </section>
    );
};

export default Hero;